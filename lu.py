# lu decomposition of a given pde - poisson 
import numpy as np
from scipy.sparse import diags
from math import e
U = np.empty([P - 1, 1])
L = np.random.rand(P - 1, P - 1) # induces a random matrix
solve_bvp(2, 5*exp**(-x), array([[-1, 2.5]]), 6)

